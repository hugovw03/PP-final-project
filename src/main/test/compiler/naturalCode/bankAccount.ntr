 /* Since we dont have a garbage bin, iteration of 100 times will cause stack overflow */
 Global Int a = 5;
 Global Lock myLock;
 RunInParallel(4) {
    Int b = 70;
    While (b IsBiggerThan 30){
        myLock.lock;
        a = a + 5;
        myLock.unlock;
        b=b-1;
    }
}